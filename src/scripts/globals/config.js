const ENV = {
  CACHE_NAME: `delicious-dish-${new Date().toISOString()}`,
  BASE_URL: `https://restaurant-api.dicoding.dev`,
  DATABASE_NAME: "delicious-dish-database",
  DATABASE_VERSION: 1,
  OBJECT_STORE_NAME: "dishes",
};

export default ENV;
